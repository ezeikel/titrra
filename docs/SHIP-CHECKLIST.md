# Titrra — Ship Checklist (what's done + what needs YOUR login)

_Status as of 2026-06-19. Generated by Claude during the kickoff session._

---

## 🆕 STORE + IAP STATUS (updated 2026-06-20)

**EAS:** project `@chewybytes/titrra` (`be088d33-0a70-4a53-8db0-ea198201c3b1`), hardcoded in `app.config.ts`. ✅

**RevenueCat:** project `proj89bc4a9f`, entitlement `titrra_pro`, offering `default`, packages `$rc_monthly`/`$rc_annual`/`$rc_lifetime` → products `titrra_pro_{monthly,yearly,lifetime}_v1`. SDK keys in `apps/mobile/.env.local`. ✅

**App Store Connect (via `asc` CLI):**
- App record `6782397414` (`com.chewybytes.titrra.app`, name "Titrra", SKU titrra) ✅
- Bundle id `9QM9X84F6T` registered with IAP capability ✅
- Subscription group `22171435` ("Titrra Pro") + group localization ✅
- **Monthly** `titrra_pro_monthly_v1` ($7.99, id `6782399132`) — created, priced, described, screenshot ✅
- **Lifetime** `titrra_pro_lifetime_v1` ($59.99, id `6782406662`) — created, priced, described, image ✅
- **Yearly** `titrra_pro_yearly_v2` ($39.99, id `6782416301`) — ✅ created. NOTE: the original `_v1` id got reserved by Apple after a delete, so we bumped to `_v2` and synced RevenueCat (RC `$rc_annual` package now → `titrra_pro_yearly_v2` = product `prodd4708c5fac`; old `_v1` RC product `prod0c6bad3575` archived). The `_v2` id is the convention's whole point. Lesson: don't `asc subscriptions delete` a sub you'll recreate — the product id gets reserved for hours+.
- All products show `MISSING_METADATA` → `READY_TO_SUBMIT` once: (a) the **Paid Applications agreement** is active (App Store Connect → Business → accept agreement + tax/banking — WEB ONLY, can't CLI), and (b) the app's first version is submitted (IAPs review alongside the binary). The review screenshots used the app icon as a placeholder — swap for real paywall screenshots before submission.

**Google Play:** ⏳ BLOCKED by design — Google has NO API to create an app. Create the app in Play Console UI + upload a first AAB, THEN `store titrra google in-app-products ...` works. The `store` wrapper already has `titrra.env` (package `com.chewybytes.titrra.app`, org key). Create the 3 matching IAPs there after first upload.

**RevenueCat credentials — key files & convention (confirmed 2026-06-20):**
- **Apple = ONE shared `.p8`, reused across all apps.** App Store Connect In-App Purchase / Subscription key `PBXJDFJ6WT` at `~/Library/Mobile Documents/com~apple~CloudDocs/Tech/App Development/Apple/AppStoreConnect-SubscriptionKey-PBXJDFJ6WT.p8`. NO new Apple key needed for Titrra — Apple IAP keys are account-level. (This `.p8` can only be *generated* in the ASC web UI; can't be made via gcloud/asc. It already exists.)
- **Google = NEW service-account JSON per app.** ✅ Generated via gcloud 2026-06-20: GCP project **`titrra`** (under org 247531169852, billing `014588-602554-6DEDCD`), SA **`revenuecat-service-account@titrra.iam.gserviceaccount.com`**, roles **Pub/Sub Editor + Monitoring Viewer**, APIs enabled (androidpublisher + playdeveloperreporting + pubsub). Key saved at `~/Library/Mobile Documents/com~apple~CloudDocs/Tech/App Development/Keys/RevenueCat/titrra-revenuecat-key.json` (matches go-unbeaten/ptp pattern).

**REMAINING MANUAL (needs your login):**
1. **Upload BOTH creds to RevenueCat dashboard** (RC MCP can't do credential upload):
   - iOS: RC → Titrra project → App Store app (`app1af97b26ae`) → upload the shared Apple `.p8` (`PBXJDFJ6WT`) + its Key ID + Issuer ID. Unblocks live iOS purchase validation.
   - Android: RC → Titrra project → Play Store app (`app4c959f9bb0`) → upload `titrra-revenuecat-key.json`. (RC Play creds take **up to 36h** to propagate — expect "invalid credentials" 503/521 until then.)
2. **Accept the Paid Applications agreement** in App Store Connect (clears MISSING_METADATA).
3. **Sandbox tester** (ASC → Users and Access → Sandbox Testers) to test a purchase in a TestFlight/dev build.
4. **Create the Titrra app in Play Console** (Google has no create-app API) + upload a first AAB, THEN invite the SA `revenuecat-service-account@titrra...` to the app with: "View app information (read-only)", "View financial data", "Manage orders and subscriptions". Then create the 3 Play IAPs matching the RC store identifiers.
5. **First build + submit** (`eas build` + `eas submit`) — iOS record exists; required to get IAPs reviewable + to seed the Play app path.

**Android build (2026-06-21):** Play app ✅ created in Console; RC service account ✅ invited + Active with the 3 permissions (View app info / View financial data / Manage orders & subscriptions); API auth verified. First `eas build --platform android --profile production` FAILED at prebuild (43s) — **missing fonts**: `app.config.ts`'s expo-font plugin referenced 5 ttf files (Inter Regular/Medium/SemiBold/Bold + GeistMono) that were never added. FIXED: copied them from go-unbeaten/apps/mobile/assets/fonts (same set, OFL-licensed), committed. Re-triggered build (id `342a8f24-2bd0-4780-b44d-76531657f433`) — cleared prebuild, in progress. ⚠️ EAS build credits at 91% this month → retries hit pay-as-you-go. NEXT after AAB completes: `eas submit --platform android` to an **internal** track (NOT production — eas.json default track is `production`, override to internal for first upload), then create the 3 Play IAPs in Console → Monetization (CLI's in-app-products endpoint is deprecated/403; new Monetization API is web-only).

---

## ✅ DONE (by Claude this session)
- [x] Name + domain decided: **Titrra** / `titrra.com` (available, $11.25/yr — NOT yet purchased, see below)
- [x] Research + v1 spec written → `docs/GLP1-RESEARCH-AND-SPEC.md`
- [x] Monorepo scaffolded (go-unbeaten pattern): `apps/web` (Next 16), `apps/mobile` (Expo 56), `packages/db`, `packages/types`
- [x] `pnpm install` succeeds (1,191 pkgs)
- [x] Prisma schema authored (full GLP-1 data model) + **validated**
- [x] **Neon project created** in Chewy Bytes org → project id `delicate-bread-39347063`
- [x] **First migration applied** (`20260619222153_init`) — 8 tables live in Neon, verified
- [x] `apps/web/.env.local` created with `DATABASE_URL` (gitignored)

## �os HUMAN-AUTH STEPS (need your login — in priority order)

### 1. Buy the domain (~$11.25/yr) — do first, it gates branding
`titrra.com` is available via Vercel Domains. Either:
- Buy in Vercel: https://vercel.com/domains/search?q=titrra.com , OR
- Tell me to and I'll drive the purchase flow (it needs your confirmation on the paid step).

### 2. One-time approve Prisma build scripts
Interactive prompt couldn't run headless:
```bash
cd ~/Development/Personal/titrra && pnpm approve-builds   # select @prisma/engines + prisma
```

### 3. Mobile binary assets
Drop into `apps/mobile/assets/` (see `apps/mobile/assets/README.md`): app icon, splash, 5 fonts. I can generate an icon via an image tool if you want — just ask.

### 4. EAS init (mints the EAS project id)
```bash
cd ~/Development/Personal/titrra/apps/mobile && eas init    # logs in to Expo, creates project
# then set EAS_PROJECT_ID in env + app.config.ts reads it
```

### 5. GitHub repo + first commit + Vercel
```bash
cd ~/Development/Personal/titrra && git init && git add -A && git commit -m "chore: scaffold titrra monorepo"
gh repo create chewy-bytes/titrra --private --source=. --push   # or your usual flow
# Vercel: link INSIDE apps/web only, Root Directory = apps/web (per go-unbeaten pattern)
cd apps/web && vercel link && vercel --prod
```
Add GitHub Actions secrets: `DATABASE_URL`, `FONTAWESOME_NPM_AUTH_TOKEN` (if used).

### 6. Stripe (web Pro unlock)
Create 3 prices in Stripe matching the pricing model:
- `titrra_pro_monthly` — $7.99/mo
- `titrra_pro_annual` — $39.99/yr (3-day trial)
- `titrra_pro_lifetime` — $59.99 one-time
Fill `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY` in `apps/web/.env.local`. Build checkout + webhook (stubbed in `apps/web/lib/stripe.ts`).

### 7. RevenueCat (mobile IAP) — MCP NOT connected this session, do manually
In RevenueCat dashboard:
- New project: **Titrra**
- Entitlement: **`titrra_pro`**
- Offering: **`default`** with packages: `$rc_monthly` ($7.99), `$rc_annual` ($39.99), `$rc_lifetime` ($59.99)
- Add iOS + Android app, paste public SDK keys into mobile env: `EXPO_PUBLIC_REVENUECAT_IOS_KEY`, `EXPO_PUBLIC_REVENUECAT_ANDROID_KEY`
- (If you reconnect the RevenueCat MCP, I can do most of this programmatically — ping me.)

### 8. App Store Connect + Play Console records (use your new store CLIs)
- **Apple:** needs the one-time ASC API key (per your `project_store_clis` memory — not yet created). Create at appstoreconnect.apple.com/access/integrations/api, then `asc auth login --name chewybytes ...`. Then create the app record (bundle `com.chewybytes.titrra.app`), add to the `store` wrapper config with its ascAppId, and put that id in `eas.json`.
- **Google:** the org key already works — register the app under the existing service account, add to `store` wrapper.
- Create the IAP products on BOTH stores matching the RevenueCat product ids.

### 9. Build + submit
```bash
cd apps/mobile
eas build --profile production --platform all
eas submit --profile production --platform all
```

## NOTES
- Neon connection string is in `apps/web/.env.local` (gitignored). For prod, add `DATABASE_URL` to Vercel env + GitHub secrets — Neon branch strategy: use `main` for prod, create a `development` branch for local once you want isolation.
- Compliance: every dose/PK screen MUST show the "not medical advice" disclaimer (already noted in CLAUDE.md + the spec).
