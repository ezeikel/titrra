# Titrra — Ship Checklist (what's done + what needs YOUR login)

_Status as of 2026-06-19. Generated by Claude during the kickoff session._

## ✅ DONE (by Claude this session)
- [x] Name + domain decided: **Titrra** / `titrra.com` (available, $11.25/yr — NOT yet purchased, see below)
- [x] Research + v1 spec written → `docs/GLP1-RESEARCH-AND-SPEC.md`
- [x] Monorepo scaffolded (go-unbeaten pattern): `apps/web` (Next 16), `apps/mobile` (Expo 56), `packages/db`, `packages/types`
- [x] `pnpm install` succeeds (1,191 pkgs)
- [x] Prisma schema authored (full GLP-1 data model) + **validated**
- [x] **Neon project created** in Chewy Bytes org → project id `delicate-bread-39347063`
- [x] **First migration applied** (`20260619222153_init`) — 8 tables live in Neon, verified
- [x] `apps/web/.env.local` created with `DATABASE_URL` (gitignored)

## �os HUMAN-AUTH STEPS (need your login — in priority order)

### 1. Buy the domain (~$11.25/yr) — do first, it gates branding
`titrra.com` is available via Vercel Domains. Either:
- Buy in Vercel: https://vercel.com/domains/search?q=titrra.com , OR
- Tell me to and I'll drive the purchase flow (it needs your confirmation on the paid step).

### 2. One-time approve Prisma build scripts
Interactive prompt couldn't run headless:
```bash
cd ~/Development/Personal/titrra && pnpm approve-builds   # select @prisma/engines + prisma
```

### 3. Mobile binary assets
Drop into `apps/mobile/assets/` (see `apps/mobile/assets/README.md`): app icon, splash, 5 fonts. I can generate an icon via an image tool if you want — just ask.

### 4. EAS init (mints the EAS project id)
```bash
cd ~/Development/Personal/titrra/apps/mobile && eas init    # logs in to Expo, creates project
# then set EAS_PROJECT_ID in env + app.config.ts reads it
```

### 5. GitHub repo + first commit + Vercel
```bash
cd ~/Development/Personal/titrra && git init && git add -A && git commit -m "chore: scaffold titrra monorepo"
gh repo create chewy-bytes/titrra --private --source=. --push   # or your usual flow
# Vercel: link INSIDE apps/web only, Root Directory = apps/web (per go-unbeaten pattern)
cd apps/web && vercel link && vercel --prod
```
Add GitHub Actions secrets: `DATABASE_URL`, `FONTAWESOME_NPM_AUTH_TOKEN` (if used).

### 6. Stripe (web Pro unlock)
Create 3 prices in Stripe matching the pricing model:
- `titrra_pro_monthly` — $7.99/mo
- `titrra_pro_annual` — $39.99/yr (3-day trial)
- `titrra_pro_lifetime` — $59.99 one-time
Fill `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY` in `apps/web/.env.local`. Build checkout + webhook (stubbed in `apps/web/lib/stripe.ts`).

### 7. RevenueCat (mobile IAP) — MCP NOT connected this session, do manually
In RevenueCat dashboard:
- New project: **Titrra**
- Entitlement: **`titrra_pro`**
- Offering: **`default`** with packages: `$rc_monthly` ($7.99), `$rc_annual` ($39.99), `$rc_lifetime` ($59.99)
- Add iOS + Android app, paste public SDK keys into mobile env: `EXPO_PUBLIC_REVENUECAT_IOS_KEY`, `EXPO_PUBLIC_REVENUECAT_ANDROID_KEY`
- (If you reconnect the RevenueCat MCP, I can do most of this programmatically — ping me.)

### 8. App Store Connect + Play Console records (use your new store CLIs)
- **Apple:** needs the one-time ASC API key (per your `project_store_clis` memory — not yet created). Create at appstoreconnect.apple.com/access/integrations/api, then `asc auth login --name chewybytes ...`. Then create the app record (bundle `com.chewybytes.titrra.app`), add to the `store` wrapper config with its ascAppId, and put that id in `eas.json`.
- **Google:** the org key already works — register the app under the existing service account, add to `store` wrapper.
- Create the IAP products on BOTH stores matching the RevenueCat product ids.

### 9. Build + submit
```bash
cd apps/mobile
eas build --profile production --platform all
eas submit --profile production --platform all
```

## NOTES
- Neon connection string is in `apps/web/.env.local` (gitignored). For prod, add `DATABASE_URL` to Vercel env + GitHub secrets — Neon branch strategy: use `main` for prod, create a `development` branch for local once you want isolation.
- Compliance: every dose/PK screen MUST show the "not medical advice" disclaimer (already noted in CLAUDE.md + the spec).
