{
  "name": "@titrra/types",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Shared Titrra TypeScript types: domain models, analytics event vocabulary, and DTOs. Consumed as TypeScript source by web (Next transpilePackages) and mobile (Metro).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "check-types": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "~6.0.3",
    "vitest": "^4.1.10"
  }
}
