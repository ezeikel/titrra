{
  "$schema": "https://turbo.build/schema.json",
  "ui": "tui",
  "globalEnv": ["FONTAWESOME_NPM_AUTH_TOKEN"],
  "tasks": {
    "transit": {
      "dependsOn": ["^transit"]
    },
    "build": {
      "dependsOn": ["test", "^build", "@titrra/db#db:generate"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": [".next/**", "!.next/cache/**", "dist/**"],
      "env": [
        "DATABASE_URL",
        "SHADOW_DATABASE_URL",
        "NEXT_PUBLIC_APP_URL",
        "NEXT_PUBLIC_POSTHOG_KEY",
        "NEXT_PUBLIC_POSTHOG_HOST",
        "NEXT_PUBLIC_SENTRY_DSN",
        "SENTRY_DSN",
        "SENTRY_AUTH_TOKEN",
        "STRIPE_SECRET_KEY",
        "STRIPE_WEBHOOK_SECRET",
        "NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY",
        "RESEND_API_KEY",
        "EXPO_PUBLIC_ENVIRONMENT",
        "EXPO_PUBLIC_APP_URL",
        "EXPO_PUBLIC_API_URL",
        "EXPO_PUBLIC_POSTHOG_KEY",
        "EXPO_PUBLIC_POSTHOG_API_KEY",
        "EXPO_PUBLIC_POSTHOG_HOST",
        "EXPO_PUBLIC_SENTRY_DSN",
        "EXPO_PUBLIC_REVENUECAT_IOS_KEY",
        "EXPO_PUBLIC_REVENUECAT_ANDROID_KEY",
        "NEXT_PUBLIC_VERCEL_ENV",
        "VERCEL_ENV",
        "VERCEL_URL",
        "WORKER_URL",
        "WORKER_SECRET",
        "CRON_SECRET",
        "NEXT_PUBLIC_SANITY_PROJECT_ID",
        "NEXT_PUBLIC_SANITY_DATASET",
        "NEXT_PUBLIC_SANITY_API_VERSION"
      ]
    },
    "dev": {
      "dependsOn": ["@titrra/db#db:generate"],
      "cache": false,
      "persistent": true
    },
    "check-types": {
      // The generated Prisma client is gitignored, so it won't exist on a fresh
      // CI checkout. tsc needs it (@titrra/db imports ./generated/prisma), so
      // generate it first — otherwise `check-types` fails with "Cannot find
      // module './generated/prisma/client'".
      "dependsOn": ["transit", "@titrra/db#db:generate"]
    },
    "db:generate": {
      "cache": false,
      "inputs": ["packages/db/prisma/schema.prisma"],
      "outputs": ["packages/db/src/generated/prisma/**"],
      "env": ["DATABASE_URL"]
    },
    "db:migrate": {
      "cache": false,
      "persistent": true
    },
    "db:deploy": {
      "cache": false
    },
    "test": {
      "dependsOn": ["^build"],
      "outputs": []
    }
  }
}
